{
  "name": "electron-preact-scratch",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel -d dist/ src/",
    "start": "npm run compile && electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-preact": "^1.1.0",
    "preact": "^8.2.9"
  },
  "dependencies": {
    "electron": "^2.0.5",
    "electron-packager": "^12.1.0"
  }
}
